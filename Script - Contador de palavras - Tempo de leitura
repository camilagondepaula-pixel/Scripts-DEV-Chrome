
(() => {
  const text = document.body.innerText;
  const words = text.trim().split(/\s+/).length;
  const minutes = Math.ceil(words / 200); // ~200 palavras por minuto
  console.log(` ${words} palavras — tempo de leitura: ~${minutes} min`);
})();
